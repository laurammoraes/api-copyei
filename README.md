# - Clonando Sites com JS

Iniciar infraestrutura
 ```
     sudo docker compose up -d
 ```

Baixar dependências

```
    npm install
    npx prisma migrate dev
    npm run seed
```

Iniciar aplicação:

```
    npm start
```
